<%= form_with(model: tuit, local: true) do |form| %>
  <% if tuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tuit.errors.count, "error") %> prohibited this tuit from being saved:</h2>

      <ul>
        <% tuit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tuit %>
    <%= form.text_area :tuit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
